# Trade Available Plugin

Restores the retail `Tradable` field omitted by the stock offline item factory.
It normalizes both saved inventory before the first sync and newly created
items, while preserving the packaged `trade_locked` artifact capsule list.

This responsibility is intentionally independent from Tamed Island and
Pioneer progression.
