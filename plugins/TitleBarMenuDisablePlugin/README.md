# Title Bar Menu Disable Plugin

Disables `TitleBarMenuGroup`, the horizontal scrollable PC menu strip displayed
inside fullscreen title bars. The regular page title, Back button and Close
button remain active.

The plugin also removes the disabled strip's right-anchor reservation so Shop
currency widgets can use the released header space.

Configuration:

- `General.Enabled = true`
